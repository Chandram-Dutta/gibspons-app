import 'package:flutter/material.dart';

class ViewMailsPage extends StatelessWidget {
  const ViewMailsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("View Mails"),
      ),
    );
  }
}
